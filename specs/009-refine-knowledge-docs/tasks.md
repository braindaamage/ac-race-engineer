# Tasks: Refine Knowledge Base Documents

**Input**: Design documents from `/specs/009-refine-knowledge-docs/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, quickstart.md

**Tests**: Not generating test tasks — no new test code required. Validation uses the existing 48 knowledge tests.

**Organization**: Tasks follow document priority order from the audit report. Each document edit is tagged with the primary user story it serves. Multiple stories are served per document since corrections, attribution fixes, parameter naming, content gaps, and consistency are interleaved within each file.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1-US6)
- Exact file paths included in all descriptions

---

## Phase 1: Setup

**Purpose**: No project initialization needed — this is a content-only change to existing files. Verify baseline.

- [x] T001 Run existing knowledge tests to confirm baseline passes: `conda run -n ac-race-engineer pytest backend/tests/knowledge/ -v`

**Checkpoint**: All 48 tests pass. Baseline confirmed.

---

## Phase 2: Foundational (Audit Reference)

**Purpose**: Read and internalize the audit report and existing document structure before making any edits.

- [x] T002 Read `docs/AUDIT_REPORT.md` and `specs/009-refine-knowledge-docs/plan.md` to understand all required corrections, their priority order, and the mapping of corrections to documents

**Checkpoint**: Implementer understands all corrections needed per document. Ready to edit.

---

## Phase 3: Priority 1 — Tyre Dynamics (US1 + US5)

**Goal**: Fix the foundational tire model description and fill critical content gaps in `tyre_dynamics.md`. This document is referenced by all others, so it must be corrected first.

**Independent Test**: Verify `tyre_dynamics.md` loads successfully, contains brush model description, load sensitivity section, pneumatic trail section, relaxation length section, and camber thrust section. Zero Pacejka references. 48 tests pass.

### Implementation

- [x] T003 [US1] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add explicit AC brush-based tire model identification at the beginning of the section (bristle deformation, adhesion/sliding regions, Coulomb friction). Note Pacejka is the industry standard but AC uses a physics-based approach instead.

- [x] T004 [US1] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add **Tire Load Sensitivity** subsection after "Wear Mechanisms": explain concave Fy-Fz relationship (effective friction coefficient decreases with load). Include conceptual example: two tires at equal 6000N load generate more total lateral force than one at 4000N and one at 8000N.

- [x] T005 [US1] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add **Pneumatic Trail and Self-Aligning Torque** subsection: pneumatic trail decreases with increasing slip angle, reaches zero/negative near adhesion limit. This is the mechanism behind steering wheel lightening near the grip limit in AC (primary FFB indicator).

- [x] T006 [US1] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add **Relaxation Length** subsection: distance tire must roll for lateral force to reach ~63% of steady-state value. AC models explicitly via RELAXATION_LENGTH parameter. Affects transient response, increases with speed and load.

- [x] T007 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add **Camber Thrust** subsection: lateral force generated by tire inclination independent of slip angle. Camber thrust explains why negative camber benefits the outer wheel during cornering.

- [x] T008 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — expand existing Combined Slip content: interaction follows approximately elliptical envelope, forces trade off continuously (not binary). Incrementing braking force reduces available lateral force progressively.

- [x] T009 [US1] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add note about AC's thermal model specifics: core temperature (higher inertia) vs surface inner/mid/outer temps. Surface temps available via Python API only (not shared memory in AC original).

- [x] T010 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/tyre_dynamics.md` — add SAT/FFB torque as tire saturation diagnostic, wheel load per wheel (`wheelLoad[4]`) for load sensitivity analysis, hot pressure monitoring (AC models pressure-temperature relationship).

**Checkpoint**: `tyre_dynamics.md` fully corrected. Run `conda run -n ac-race-engineer pytest backend/tests/knowledge/ -v` to confirm 48 tests pass.

---

## Phase 4: Priority 2 — Vehicle Balance Fundamentals (US2 + US5)

**Goal**: Fix the springs/dampers causal attribution error and fill weight transfer decomposition gaps in `vehicle_balance_fundamentals.md`.

**Independent Test**: Verify document loads, contains correct attribution (springs/ARBs = steady-state LLTD; dampers = transient rate), weight transfer decomposition section, TLLTD concept, and no "ramp angles" as AC parameters.

### Implementation

- [x] T011 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — expand existing "Neutral Steer and Load Sensitivity" paragraph into a more prominent explanation: tire load sensitivity is THE mechanism by which weight transfer distribution affects balance. The concave Fy-Fz curve means the more heavily loaded axle loses proportionally more grip.

- [x] T012 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — add **Weight Transfer Decomposition** subsection: three components — geometric (via roll center, instantaneous), elastic (via springs/ARBs, proportional to roll stiffness distribution), unsprung mass. Note ride height affects roll center position.

- [x] T013 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — add **TLLTD** concept: Total Lateral Load Transfer Distribution unifies springs, ARBs, and suspension geometry effects. Approximate rule: TLLTD front is roughly static weight distribution front + ~5%.

- [x] T014 [US2] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — add **Transient vs Steady-State Balance** subsection: clearly separate steady-state (springs/ARBs control LLTD magnitude and distribution) from transient (dampers control rate of transfer). Different phenomena requiring different tuning approaches.

- [x] T015 [US2] Edit Adjustable Parameters — Spring Rates in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — refine wording to clarify that stiffer springs increase roll stiffness at that axle (shifting LLTD) but do NOT control the speed/rate of weight transfer (that is dampers). Distinguish body roll rate from load transfer rate explicitly.

- [x] T016 [US3] Edit Adjustable Parameters — Differential Settings in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — replace "high-ramp-angle" with "high lock percentage" to match AC terminology. Note AC uses POWER/COAST lock percentages (0.0-1.0).

- [x] T017 [US3] Edit Cross-References — drivetrain.md entry in `backend/ac_engineer/knowledge/docs/vehicle_balance_fundamentals.md` — replace "ramp angles and preload" with "lock percentages and preload".

**Checkpoint**: `vehicle_balance_fundamentals.md` fully corrected. Run tests.

---

## Phase 5: Priority 3 — Telemetry and Diagnosis (US4)

**Goal**: Fix the sample rate error, add diagnostic techniques, and reframe the symptom-cause table in `telemetry_and_diagnosis.md`.

**Independent Test**: Verify document loads, states physics ~333 Hz / logging 30-200 Hz, includes G-G diagram methodology, includes reference lap methodology, symptom-cause table reframed as hypothesis generator.

### Implementation

- [x] T018 [US4] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — correct sample rate: replace "approximately 20-30 Hz" with accurate description (physics engine ~333 Hz internal; telemetry tools log at 30-200 Hz depending on tool and configuration). Update the sampling interval math accordingly.

- [x] T019 [US4] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — add **Telemetry Tools Ecosystem** subsection: mention ACTI (legacy ~20 Hz), Telemetrick (30-200 Hz, 180+ channels, recommended primary tool), MoTeC i2 Pro (analysis software). Note Telemetry Exchange for comparison with other drivers.

- [x] T020 [US4] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — add **G-G Diagram** subsection: plot lateral vs longitudinal G to visualize grip utilization and identify under-used areas of the performance envelope. Fundamental diagnostic technique.

- [x] T021 [US4] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — add **Reference Lap Methodology** subsection: compare against reference (fastest personal lap or another driver via Telemetry Exchange). Overlay traces, identify where time is gained/lost by braking point, corner speed, or exit speed.

- [x] T022 [US4] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — reframe the symptom-cause table: update heading and introductory text to present as "hypothesis generator" not definitive diagnosis. Add note about equifinalidad (multiple causes produce identical symptoms), speed dependence (mechanical vs aero origins), and driver technique potentially masking or mimicking setup problems.

- [x] T023 [US3] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/telemetry_and_diagnosis.md` — add note where brake duct is mentioned that it is car-specific (not available on all AC cars).

**Checkpoint**: `telemetry_and_diagnosis.md` fully corrected. Run tests.

---

## Phase 6: Priority 4 — Suspension and Springs (US2 + US3 + US5)

**Goal**: Fix the critical spring rate vs wheel rate confusion, correct the springs-transfer-speed error, and add missing content in `suspension_and_springs.md`.

**Independent Test**: Verify document loads, explicitly states AC SPRING_RATE = wheel rate, no "springs govern the transient rate" claim, includes natural frequency table, ride height-roll center section, and bump stop tuning content.

### Implementation

- [x] T024 [US2] Edit Physical Principles — "Spring rate and load transfer speed" in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — fix the core error: replace "springs govern the transient rate of transfer, not the final magnitude" with the correct attribution: springs and ARBs determine the magnitude and distribution of steady-state lateral load transfer (LLTD); dampers control the transient rate.

- [x] T025 [US3] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — add prominent AC-specific note: "In Assetto Corsa, the SPRING_RATE parameter in setup files and in-game adjustment screens represents wheel rate (stiffness measured at the wheel), not spring rate at the physical coil spring. Wheel rate = spring rate x (motion ratio)^2. All values in AC refer to wheel rate."

- [x] T026 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — add **Ride Height, Roll Center, and Balance** subsection: changing ride height shifts roll center position, altering the geometric component of lateral load transfer. This makes ride height a powerful balance tuning tool beyond CG height and aero effects.

- [x] T027 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — add **Anti-Geometry** brief subsection: anti-dive, anti-squat from suspension geometry. Not user-adjustable in AC but explains why different cars react differently to braking/acceleration pitch.

- [x] T028 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — add natural frequency reference table by vehicle category (street 0.5-1.5 Hz, rally 1.5-2.0, non-aero race 1.5-2.5, GT3 2.5-3.5, high downforce 3.5-5.0+) with formula f = (1/2pi) * sqrt(K_wheel / m_corner_sprung).

- [x] T029 [US5] Edit Physical Principles and Adjustable Parameters in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — expand bump stop content: note AC models bump stops with progressive rate (BUMPSTOP_RATE, BUMPSTOP_UP). In aero cars, bump stops are active tuning elements for platform control, not just emergency travel limiters.

- [x] T030 [US3] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — update spring rate description to clarify AC uses wheel rates. Add note that motion ratios are not an explicit AC parameter (derived from suspension geometry coordinates).

- [x] T031 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/suspension_and_springs.md` — add bump stop contact events (identifiable by abrupt spikes in wheel load or travel hitting limits) and pitch angle (front-rear ride height differential) diagnostics.

**Checkpoint**: `suspension_and_springs.md` fully corrected. Run tests.

---

## Phase 7: Priority 5 — Drivetrain (US3)

**Goal**: Fix the ramp angle terminology to AC's lock percentage model in `drivetrain.md`.

**Independent Test**: Verify document loads, Physical Principles uses lock percentages (not ramp angles as AC parameters), 1-way/1.5-way/2-way described with lock percentages, preload separated from lock behavior, Torsen note present.

### Implementation

- [x] T032 [US3] Edit Physical Principles — LSD subsection in `backend/ac_engineer/knowledge/docs/drivetrain.md` — rewrite to replace all "ramp angle" / "ramp configuration" terminology with AC's lock percentage model. Explain: AC models a clutch-pack LSD with POWER and COAST parameters (0.0 = fully open, 1.0 = fully locked). Ramp angles are a real-world concept not directly represented in AC.

- [x] T033 [US3] Edit Physical Principles — 1-way/1.5-way/2-way descriptions in `backend/ac_engineer/knowledge/docs/drivetrain.md` — rewrite using lock percentages: 1-way = COAST near 0, POWER > 0; 1.5-way = COAST < POWER (typically COAST ~0.4-0.6 x POWER); 2-way = COAST = POWER. Clarify 2-way means equal lock percentage, NOT fully locked.

- [x] T034 [US3] Edit Physical Principles — "Power and Coast Ramp Angles" bold subheading in `backend/ac_engineer/knowledge/docs/drivetrain.md` — rename to "Power and Coast Lock Behavior" and rewrite content to describe lock percentages. Note preload (Nm) is constant base locking torque independent of throttle/brake, while POWER/COAST lock is proportional to drivetrain torque.

- [x] T035 [US3] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/drivetrain.md` — add note that AC does not model Torsen/helical gear differentials explicitly. The clutch-type model can approximate general behavior but lacks speed-differential sensitivity characteristic of helical gears.

- [x] T036 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/drivetrain.md` — add brief gear ratio optimization guidance: RPM drop between gears, keeping engine in peak power band, final drive as first adjustment for new circuits.

- [x] T037 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/drivetrain.md` — add wheel speed ratio (not just difference) as diagnostic for diff locking state. Add correlation of throttle application rate with wheelspin onset for diff behavior diagnosis.

**Checkpoint**: `drivetrain.md` fully corrected. Run tests.

---

## Phase 8: Priority 6 — Braking (US3)

**Goal**: Add car-specific parameter availability notes and fill content gaps in `braking.md`.

**Independent Test**: Verify document loads, brake ducts/pad compounds/engine brake noted as car-specific, brake fade noted as car-specific, deceleration G reference present.

### Implementation

- [x] T038 [US3] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/braking.md` — add car-specific availability note to brake duct section: "Available on some cars in AC; not universal. In ACC, all cars have adjustable brake duct size (scale 0-6)."

- [x] T039 [US3] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/braking.md` — add car-specific note to pad compound section: "Not available as a setup parameter in AC original. ACC offers 4 pad compound options."

- [x] T040 [US3] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/braking.md` — add car-specific note to engine brake section: "Adjustable only on specific cars (formula cars, some GTs with ECU). Not a universal parameter."

- [x] T041 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/braking.md` — add physics of lock-up (transition from dynamic to kinetic friction, ~10-30% grip reduction), deceleration G reference by category (street 0.8-1.0g, GT3 1.2-1.5g, open-wheel 2.0-3.5g, F1 4.0-6.0g).

- [x] T042 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/braking.md` — expand trail braking physics: maintaining partial brake pressure during turn-in keeps weight on front axle (preserving front grip) and exploits friction ellipse for simultaneous lateral + longitudinal force.

- [x] T043 [US5] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/braking.md` — add brake bias ideal calculation concept (front load under braking = static front weight + mass x deceleration x CG height / wheelbase). Add note that brake fade is modeled only for specific classic cars in AC; most modern cars have unlimited brake endurance.

- [x] T044 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/braking.md` — add individual wheel speed differential during braking as diagnostic for effective brake bias. Add brake pressure vs deceleration G correlation for fade detection.

**Checkpoint**: `braking.md` fully corrected. Run tests.

---

## Phase 9: Priority 7 — Dampers (US2 + US5)

**Goal**: Add AC-specific damper model details, velocity domains, and performance metrics to `dampers.md`.

**Independent Test**: Verify document loads, AC bilineal model with parameter names present, velocity domain framework present, damping ratio targets present, rebound:compression ratio guidance present.

### Implementation

- [x] T045 [P] [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/dampers.md` — add AC's bilineal damping model description with actual parameter names: DAMP_BUMP, DAMP_FAST_BUMP, DAMP_FAST_BUMPTHRESHOLD (and rebound equivalents). Two-slope model with knee point. Note values in AC are expressed at the wheel.

- [x] T046 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/dampers.md` — add **Velocity Domain Framework** subsection: low-speed (0-50 mm/s, body control — most critical), mid-speed (50-200 mm/s, transitions), high-speed (200+ mm/s, bump absorption — must be soft enough for tire contact).

- [x] T047 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/dampers.md` — add damping ratio targets: race cars typically zeta 0.65-0.7 (minimizes settling time with minimal overshoot), street cars 0.2-0.5, critical damping zeta=1.0 eliminates overshoot but slower to settle than 0.7.

- [x] T048 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/dampers.md` — add rebound:compression ratio guidance: typical competition 1.5:1 to 3:1. Rebound controls rate of load release. Excessive rebound causes "packing down" (suspension not fully extending between compressions).

- [x] T049 [US5] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/dampers.md` — add wheel load variation as key damper performance metric (minimizing fluctuation maximizes average grip via tire load sensitivity). Add platform control concept for aero cars.

- [x] T050 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/dampers.md` — add damper velocity histograms (time distribution across velocity domains) and wheel load coefficient of variation (std dev / mean per wheel) as diagnostics.

**Checkpoint**: `dampers.md` fully corrected. Run tests.

---

## Phase 10: Priority 8 — Alignment (US3 + US5)

**Goal**: Clarify adjustable vs fixed parameters and add dynamic camber content in `alignment.md`.

**Independent Test**: Verify document loads, clear distinction of adjustable vs emergent geometry, dynamic camber section present, camber reference values present.

### Implementation

- [x] T051 [P] [US3] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/alignment.md` — add clear distinction: "In AC, camber and toe are adjustable in the setup screen. Caster is adjustable only on some cars. KPI, scrub radius, and Ackermann geometry are emergent properties of suspension geometry in suspensions.ini — editable only by modders."

- [x] T052 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/alignment.md` — add **Dynamic Camber** subsection: static camber is the starting point; what matters is camber under cornering load. Three contributors: geometry gain (FVSA), caster-induced camber, suspension travel dependent. AC models all via kinematic solver.

- [x] T053 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/alignment.md` — add **Camber Thrust** subsection: lateral force from tire inclination independent of slip angle. Add typical camber reference values (street -0.5 to -1.5 deg, GT/touring -2.0 to -3.5, open-wheel -2.5 to -4.0).

- [x] T054 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/alignment.md` — add brief Ackermann concept (100% for low speed, reduced/anti for high speed with slip angles) and bump steer note (modeled in AC, emerges from steering rod geometry, not adjustable).

- [x] T055 [US5] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/alignment.md` — add toe trade-off specifics: front toe-out increases turn-in but reduces straight-line stability; rear toe-in universally used in competition (0.5-1.5mm total).

- [x] T056 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/alignment.md` — add thermal gradient inner-to-outer as primary camber diagnostic (ideal ~5-10 deg C hotter inner under load) and steering angle vs lateral G scatter plot for Ackermann effects.

**Checkpoint**: `alignment.md` fully corrected. Run tests.

---

## Phase 11: Priority 9 — Aero Balance (US5)

**Goal**: Add AC-specific aero model details, CoP concept, and efficiency metrics to `aero_balance.md`.

**Independent Test**: Verify document loads, AC independent wing-element model described, V-squared scaling stated, CoP vs CoG section present, L/D ratio present.

### Implementation

- [x] T057 [P] [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add **AC's Aero Model** subsection: each element is an independent "wing" with own CL, CD, AOA and ground height lookup tables (LUT_GH_CL, LUT_GH_CD). Elements do NOT interact. Significant simplification vs reality and vs ACC.

- [x] T058 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add explicit V-squared force scaling statement and diagnostic implication: compare balance in slow vs fast corners to isolate aero vs mechanical contribution.

- [x] T059 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add **Center of Pressure vs Center of Gravity** subsection: if CoP != CoG, downforce creates pitch moment changing with V-squared. Aero balance = front downforce / total downforce.

- [x] T060 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add **Aerodynamic Efficiency (L/D)** subsection: downforce-to-drag ratio. Add notes on yaw sensitivity (YAW_CL_GAIN, YAW_CD_GAIN) and dynamic controllers ([DYNAMIC_CONTROLLER] for active aero in AC).

- [x] T061 [US5] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add aero platform stability concept: consistent ride height under all conditions is critical; springs, anti-dive, bump stops contribute.

- [x] T062 [US5] Edit Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/aero_balance.md` — add balance vs speed plot as key diagnostic and front/rear ride height under aero load.

- [x] T063 [US5] Edit Cross-References in `backend/ac_engineer/knowledge/docs/aero_balance.md` — ensure references to suspension (ride height → platform), dampers (platform control), and braking (aero effect on bias) are present.

**Checkpoint**: `aero_balance.md` fully corrected. Run tests.

---

## Phase 12: Priority 10 — Setup Methodology (US5)

**Goal**: Add OVAT limitations, increase lap count, add sensitivity hierarchy, and correct terminology in `setup_methodology.md`.

**Independent Test**: Verify document loads, OVAT limitations acknowledged, lap count 8-10, sensitivity hierarchy present, "ramp angles" replaced with "lock settings", driver adaptation noted.

### Implementation

- [x] T064 [P] [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — add acknowledgment of OVAT limitations: does not detect interaction effects. Mention simplified DOE as alternative for advanced users.

- [x] T065 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — add **Parameter Sensitivity Hierarchy** subsection: high (ride height, spring rates, wing angle, pressures), medium (ARB, dampers, diff, brake bias), low (camber fine-tuning, toe, individual gear ratios).

- [x] T066 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — add **Interaction Effects** subsection: key interactions (spring-damper, ride height-aero, pressure-camber, diff-spring/ARB). Note: changing springs invalidates optimal damper settings.

- [x] T067 [US5] Edit Physical Principles in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — add **Driver Adaptation as Confound** note: driver unconsciously adapts to setup changes. First laps on new setup unreliable. Recommend adaptation laps before timed comparison.

- [x] T068 [US5] Edit Adjustable Parameters in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — update priority order to: ride height/aero platform, pressures/temps, gross balance (springs, ARBs), damping, fine balance (diff, brake bias), fine-tuning (camber, toe, final pressures). Replace "differential ramp angles" with "differential lock settings".

- [x] T069 [US5] Edit Adjustable Parameters and Telemetry Diagnosis in `backend/ac_engineer/knowledge/docs/setup_methodology.md` — increase recommended lap count from 3-5 to 8-10 minimum per configuration. Recommend median over mean. Add A/B test protocol guidance and sensitivity plot concept.

**Checkpoint**: `setup_methodology.md` fully corrected. Run tests.

---

## Phase 13: Index Update and Final Validation (US6)

**Goal**: Update KNOWLEDGE_INDEX tags for new content areas and verify cross-document consistency.

**Independent Test**: All 48 tests pass. Grep across all documents confirms zero instances of: incorrect tire model refs, springs-transfer-speed attribution, wrong AC parameter names, incorrect sample rates.

### Implementation

- [x] T070 [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `tyre_dynamics.md` Physical Principles: add `"brush model"`, `"pneumatic trail"`, `"self-aligning torque"`, `"relaxation length"`, `"camber thrust"`

- [x] T071 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `vehicle_balance_fundamentals.md` Physical Principles: add `"TLLTD"`, `"roll center"`, `"transient"`, `"steady state"`, `"weight transfer decomposition"`

- [x] T072 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `suspension_and_springs.md` Physical Principles: add `"wheel rate"`, `"anti-dive"`, `"anti-squat"`, `"roll center"`

- [x] T073 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `dampers.md` Physical Principles: add `"damping ratio"`, `"velocity domain"`, `"platform control"`

- [x] T074 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — update KNOWLEDGE_INDEX tags for `drivetrain.md` Physical Principles: replace `"power ramp"`, `"coast ramp"` with `"power lock"`, `"coast lock"`, `"lock percentage"`. Add `"trail braking"`.

- [x] T075 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `alignment.md` Physical Principles: add `"dynamic camber"`, `"camber thrust"`, `"ackermann"`

- [x] T076 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `aero_balance.md` Physical Principles: add `"center of pressure"`, `"lift to drag"`, `"efficiency"`

- [x] T077 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `telemetry_and_diagnosis.md` Telemetry Diagnosis: add `"g-g diagram"`, `"friction circle"`, `"reference lap"`

- [x] T078 [P] [US6] Edit `backend/ac_engineer/knowledge/index.py` — expand KNOWLEDGE_INDEX tags for `setup_methodology.md` Physical Principles: add `"interaction effects"`, `"parameter sensitivity"`

- [x] T079 [US6] Run full knowledge test suite: `conda run -n ac-race-engineer pytest backend/tests/knowledge/ -v` — all 48 tests must pass

- [x] T080 [US6] Cross-document consistency verification: grep all 10 docs for "ramp angle" (should only appear as real-world context, never as AC parameter), "springs govern.*transient rate" (zero matches), "20-30 Hz" or "20–30 Hz" (zero matches), "brake duct" in braking.md (must have availability note). Fix any inconsistencies found.

**Checkpoint**: All documents refined, index updated, tests passing, cross-document consistency verified.

---

## Phase 14: Polish & Cross-Cutting Concerns

**Purpose**: Final cleanup and full validation

- [x] T081 Run full project test suite to confirm no regressions: `conda run -n ac-race-engineer pytest backend/tests/ -v`
- [x] T082 Review all 10 documents for consistent prose style (bold subheadings, paragraph-based, 3-layer approach: physics → metrics → setup levers)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: No dependencies — baseline test run
- **Phase 2 (Foundational)**: Depends on Phase 1
- **Phase 3 (tyre_dynamics)**: Depends on Phase 2 — MUST be first document edited (foundation for all others)
- **Phase 4 (vehicle_balance)**: Depends on Phase 3 (references tyre model)
- **Phase 5 (telemetry)**: Can run after Phase 2 (independent of Phases 3-4)
- **Phase 6 (suspension)**: Can run after Phase 4 (references balance concepts)
- **Phase 7 (drivetrain)**: Can run after Phase 2 (independent content)
- **Phase 8 (braking)**: Can run after Phase 2 (independent content)
- **Phase 9 (dampers)**: Can run after Phase 4 (references balance/transfer concepts)
- **Phase 10 (alignment)**: Can run after Phase 3 (references tyre model)
- **Phase 11 (aero)**: Can run after Phase 4 (references balance concepts)
- **Phase 12 (methodology)**: Can run after Phase 2 (independent content)
- **Phase 13 (index + validation)**: Depends on ALL document phases (3-12) complete
- **Phase 14 (polish)**: Depends on Phase 13

### User Story Dependencies

- **US1 (Tire Model)**: Phase 3 only — no dependencies on other stories
- **US2 (Springs/Dampers)**: Phases 4, 6, 9 — depends on US1 (tyre model correct first)
- **US3 (AC Parameters)**: Phases 4, 5, 6, 7, 8, 10 — can start after Phase 2
- **US4 (Telemetry)**: Phase 5 only — can start after Phase 2
- **US5 (Content Gaps)**: Phases 3-12 — interleaved with all document edits
- **US6 (Consistency)**: Phase 13 — depends on all other stories complete

### Parallel Opportunities

After Phase 4 (vehicle_balance) completes, several phases can run in parallel:

```
Phase 5 (telemetry) ─┐
Phase 7 (drivetrain) ─┤
Phase 8 (braking) ────┤── Can all run in parallel
Phase 12 (methodology)┘

Phase 6 (suspension) ─┐
Phase 9 (dampers) ────┤── Can run in parallel after Phase 4
Phase 10 (alignment) ─┤
Phase 11 (aero) ──────┘
```

Within Phase 13, all index tag update tasks (T070-T078) can run in parallel since they edit different entries in the same file (non-overlapping dict keys).

---

## Parallel Example: Phase 13 Index Updates

```bash
# All index tag updates can be launched together:
Task: "Expand tyre_dynamics.md tags in index.py"        # T070
Task: "Expand vehicle_balance tags in index.py"          # T071
Task: "Expand suspension_and_springs tags in index.py"   # T072
Task: "Expand dampers.md tags in index.py"               # T073
Task: "Update drivetrain.md tags in index.py"            # T074
Task: "Expand alignment.md tags in index.py"             # T075
Task: "Expand aero_balance.md tags in index.py"          # T076
Task: "Expand telemetry tags in index.py"                # T077
Task: "Expand setup_methodology tags in index.py"        # T078
```

---

## Implementation Strategy

### MVP First (US1 — Tire Model Correction)

1. Complete Phase 1: Baseline test run
2. Complete Phase 2: Read audit report
3. Complete Phase 3: Fix tyre_dynamics.md
4. **STOP and VALIDATE**: Verify document loads, brush model present, tests pass
5. This alone fixes the highest-impact error in the knowledge base

### Incremental Delivery

1. Phase 3: tyre_dynamics.md corrected (US1 complete)
2. Phase 4: vehicle_balance corrected (US2 partial — attribution fixed)
3. Phase 5-6: telemetry + suspension corrected (US3 + US4 partial)
4. Phase 7-8: drivetrain + braking corrected (US3 complete)
5. Phase 9-12: dampers + alignment + aero + methodology (US5 complete)
6. Phase 13: index + consistency (US6 complete)
7. Each phase is independently testable — run 48 tests after each

### Sequential Execution (Single Developer)

Follow phases 1 through 14 in order. After each document checkpoint, run tests. Total: 82 tasks across 14 phases.

---

## Notes

- All edits are content-only within existing markdown sections — no structural changes
- Section titles (`## Physical Principles`, etc.) must NEVER be changed
- Document filenames must NEVER be changed
- The 3-layer content approach (physics → metrics → setup levers) must be maintained in all new content
- Follow existing prose style: bold subheadings within sections, paragraph-based, not bullet-heavy
- Run `conda run -n ac-race-engineer pytest backend/tests/knowledge/ -v` after each document phase
- Template files (car_template.md, track_template.md) and user docs are out of scope
